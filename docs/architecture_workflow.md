# AWS Architecture Workflow

![AWS workflow](assets/diagrams/workflow.svg)

The bottom control lane shows where deletion protection, duplicate suppression, and intrusion detection are embedded into the main workflow rather than treated as separate afterthoughts.
